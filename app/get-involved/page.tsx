import WorkInProgress from "@/components/wip";

export default function GetInvolved() {
    return (
        <WorkInProgress
            features={["Details about how to get involved", "Etc..."]}
        />
    );
}
